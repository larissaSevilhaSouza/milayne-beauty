<!-- filepath: /home/lari/Documentos/projetos-pessoais/milaine-beauty/app/views/home/_schedule_your_appointment.html.erb -->
<% @schedule.each do |schedule| %>
  <div class="max-w-3xl mx-auto my-5 bg-white rounded-lg shadow-md">
    <div class="flex flex-col md:flex-row items-center">
      <div class="w-full md:w-1/2">
        <%= image_tag schedule.photo_url, class: "w-full h-auto object-cover rounded-t-lg md:rounded-l-lg md:rounded-t-none" %>
      </div>
      <div class="w-full md:w-1/2 p-5 flex flex-col gap-5">
        <div class="text-2xl font-bold text-gray-800">
          <%= schedule.description %>
        </div>
        <div>
          <%= button_to "Agende seu horário", 
              "https://wa.me/#{format_phone_number(schedule.number)}", 
              method: :get, 
              target: "_blank",
              class: "bg-blue-900 text-white py-3 px-6 rounded-lg font-semibold w-full hover:bg-blue-700 transition-colors" 
          %>
        </div>
      </div>
    </div>
  </div>
<% end %>