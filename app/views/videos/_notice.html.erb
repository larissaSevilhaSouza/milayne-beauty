<%
=begin%>
 <% if @most_viewed_posts.present? %> 
<%
=end%>
  <%
=begin%>
 <div class="p-6 bg-white shadow-md rounded border border-gray-100">
    <h2 class="text-2xl font-bold mb-4 text-[<%= site_colors.primary %>]" style="font-family: <%= FontPrimary.first.category_font.title %>;">Notícias Mais Acessadas</h2>
    <ul class="">
      <%# @most_viewed_posts.each do |post| %>
        <li class="border-b border-gray-200 py-4 flex items-center gap-2">
          <%#= link_to post_path(post.slug), class: "hover:text-[#{site_colors.primary}] flex md:flex-col lg:flex-row items-center gap-2 hover:scale-110 transition duration-300 w-full", target: "_blank" do %>
            <div class="w-1/2 md:w-full lg:w-1/2 min-h-24 flex items-center justify-center">
              <%#= image_tag post.cover.cover.url, class: "w-full h-full" %>
            </div>
            <div class="w-1/2 md:w-full lg:w-1/2 hover:text-[<%#= site_colors.primary %>] text-[<%#= site_colors.terciary %>] flex flex-col" style="font-family: <%#= FontPrimary.first.category_font.title %>;">
              <span class="line-clamp-1 text-xs font-bold">
                <%#= post.post_category.name %>
              </span>
              <span class="line-clamp-2">
                <%#= post.title %>
              </span>
              <span class="text-sm">
                <%#= post.formatted_date_publish %>
              </span>
            </div>
          <% end %>
        </li>
      <% end %>
    </ul>
  </div>
<% end %> 
<%
=end%>
